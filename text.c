#include<stdio.h>

#include"flx.h"

int main()
{

    printf("hello world\n");
    printf("the num \t of flx: %d", FLX);
    getchar();  // 等待用户按下回车才退出
    printf("the num \t of flx: %d", FLX);
    getchar();  // 等待用户按下回车才退出
    printf("the num \t of flx: %d", FLX);
    getchar();  // 等待用户按下回车才退出
    // return 0;
}